name: kaslana-protocol
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pyyaml>=6.0.1
  - pytest>=8.0
  - pytest-asyncio>=0.23
  - ruff>=0.5
  - numpy
  - pip:
      - httpx
      - requests
      - sounddevice
      - soundfile
      - uiautomator2
